# Fixture manifest

Regenerated by `tools/gen_asr_fixtures.py`. Audio files themselves are gitignored; this file
documents what the generator produces so a re-run is reproducible.

| Path | Class | Duration (approx) | Serves | How produced |
|---|---|---|---|---|
| `nonspeech/music/layered_tones.wav` | music | 90 s | **R37** | Layered detuned sines + slow AM (programmatic) |
| `nonspeech/music/noise_bed.wav` | music | 60 s | **R37** | White noise through a one-pole low-pass (programmatic) |
| `nonspeech/chime/single_chimes.wav` | chime | 45 s | **R37** | Periodic high sine bursts with silence gaps |
| `nonspeech/chime/glass_cascade.wav` | chime | 30 s | **R37** | Descending tone cluster |
| `nonspeech/keyboard/click_burst.wav` | keyboard | 60 s | **R37** | Short noise clicks at irregular intervals |
| `nonspeech/keyboard/typing_run.wav` | keyboard | 60 s | **R37** | Faster click train |
| `speech/en/briefing_en.wav` | en | ~15 s | **R8** | `say -v Samantha` (macOS TTS) → 16 kHz WAV |
| `speech/zh/briefing_zh.wav` | zh | ~15 s | **R8**, **R10** | `say` with a zh-* voice when available, else Meijia/Tingting |
| `speech/code_switch/en_zh_interleaved.wav` | code_switch | ~20 s | **R8** | Concatenated EN then ZH TTS segments |
| `speech/en/v52_ten_line_en.wav` | en (V52) | ~3.5–5 min | **V52** / 7.3 | `tools/gen_v52_prompt_audio.py` — 10 lines × **3** passes, 4 s gaps (`say` TTS; optional `--teleprompter` mic) |

All clips: **16 kHz**, mono, PCM WAV (int16 on disk; harness loads as float32).

To regenerate after editing the script:

```bash
.venv/bin/python tools/gen_asr_fixtures.py --force
.venv/bin/python tools/gen_v52_prompt_audio.py --force
```
