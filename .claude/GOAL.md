# The goal

**Read [`STATE.md`](../STATE.md) — its handoff section is the goal, and it is rewritten as the
work moves.** This file is a pointer and nothing else.

That is not laziness. This project has spent a week correcting documents that described a
decision after it had been reversed: a plan section still asking for three-band advisor routing
the day after it was dissolved, another still requiring 48 kHz capture after the archive rate was
reversed to 16 kHz, a decision record inheriting a reversal it never heard about, and a
post-meeting prompt telling an agent about a 0.4 s flush in a file whose own header said 1.2 s.
A second place stating the goal is a second place to go stale, and it goes stale the same way
they did — quietly, while reading as current.

**This file has already proved that.** Written on 2026-08-17 with a paragraph of status attached —
"almost none of it has met real data", listing five things — it was wrong within two hours, because
two of the five were verified that morning. The paragraph is gone. What is left cannot rot.

One pointer. `STATE.md`.
